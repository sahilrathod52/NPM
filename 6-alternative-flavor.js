// We are setting up the module exports property to an array
module.exports.items = ['item1', 'item2'];

const person = {
    name: 'Hulk'
}

// We are setting up the module exports property to the person object
module.exports.singlePerson = person